name: new_piyawat
description: "A new Flutter project."

publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: ^3.9.2

dependencies:
  flutter:
    sdk: flutter
  geolocator: ^9.0.2
  flutter_map: ^6.1.0
  latlong2: ^0.9.1
  youtube_player_flutter: ^9.0.0
  google_maps_flutter: ^2.3.0
  url_launcher: ^6.2.1
  firebase_core: ^4.3.0
  cloud_firestore: ^6.1.1

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0
  video_player: ^2.8.1
  flutter_launcher_icons: ^0.13.1

flutter:
  uses-material-design: true

  # 🔹 เพิ่ม assets รูปอาหาร
  assets:
    - assets/images/
    - assets/team/

flutter_icons:
  android: true
  ios: true
  image_path: "assets/discord.png"
